server:
  port: 3355

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      name: config
      profile: dev
      uri: http://config3344:3344
      
eureka:
  client:
    server-url:
      defaultZone: http://eureka7001:7001/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"