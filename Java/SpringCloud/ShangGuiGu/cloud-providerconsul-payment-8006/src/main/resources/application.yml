server:
  port: 8006

spring:
  application:
    name: consul-provider-payment
  cloud:
    consul:
      host: 169.0.0.2
      port: 8500
      discovery:
        service-name: ${spring.application.name}